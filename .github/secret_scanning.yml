# BrassCoders test suite contains intentional secret-shaped placeholders
# (with "FIXTURE" markers) to exercise the secret-detection logic. They
# are not real credentials; excluded from secret scanning to avoid
# noisy alerts and false-positive push protection.
paths-ignore:
  - "tests/**"
