# BrassCoders test fixtures contain intentional secret-shaped placeholders
# (marked with "FIXTURE") to exercise the redaction logic. They are not
# real credentials. paths-ignore reduces alert noise; the highest-density
# fixture files are excluded from this public mirror entirely (see the
# sync script in the private monorepo).
paths-ignore:
  - "tests/**"
